import { combineReducers } from '@reduxjs/toolkit';
// Import reducers once you create them
// import authReducer from '../auth/authSlice';

export const rootReducer = combineReducers({
  // auth: authReducer,
  // Add more reducers here
});